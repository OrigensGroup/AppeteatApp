import styled from 'styled-components/native';

export const HeartLikeContainer = styled.View`
  background: ${({ theme }) => theme.colors.border}88;
  padding: ${({ theme }) => theme.spacing.single};
  border-radius: 1000px;
`;
